# Для сборки Docker-образа: без mcp[cli] (конфликт httpx; MCP на хосте).
# Вместо googletrans — deep-translator (конфликт httpx с python-telegram-bot); код в newsbot поддерживает оба.

# Core dependencies
requests>=2.31.0
python-dotenv>=1.0.0

# YAML configuration
PyYAML>=6.0.1

# Logging and monitoring
colorlog>=6.7.0

# Data validation
pydantic>=2.5.0

# Date and time
python-dateutil>=2.8.2

# Spambot (NewsBot) dependencies
python-telegram-bot>=21.0
feedparser>=6.0
deep-translator>=1.11.0

# Post FLOW (Google Sheets + GRS AI + Telegram)
gspread>=6.0.0
google-auth>=2.25.0
