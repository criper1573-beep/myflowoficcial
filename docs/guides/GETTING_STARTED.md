# Быстрый старт с ContentZavod

Это руководство поможет вам быстро начать работу с проектом ContentZavod.

### Имя папки проекта (латиница)

**Папка проекта должна называться `ContentZavod`** (латиница). Так избегаются ошибки кодировки в терминале (PowerShell, cmd) и в путях. Если папка у вас называется по-русски («КонтентЗавод» и т.п.), переименуйте её в `ContentZavod` в Проводнике и заново откройте проект в Cursor.

## 📋 Что уже готово

✅ **GRS AI Client** - универсальный блок для работы с AI API  
✅ **Блок «Проекты»** - мультипроектность (один завод — несколько бизнесов)  
✅ **Спамбот (NewsBot)** - RSS → Telegram с выбором проекта  
✅ **Документация** - полное описание архитектуры и API  
✅ **Примеры использования** - готовые шаблоны блоков  
✅ **Тестовый скрипт** - для проверки работы AI клиента  

## 🚀 Установка и настройка

### Шаг 1: Установите зависимости

```bash
pip install -r docs/config/requirements.txt
```

Это установит:
- `requests` - для HTTP запросов
- `python-dotenv` - для работы с переменными окружения
- `PyYAML` - для конфигурации
- `pydantic` - для валидации данных

### Шаг 2: Настройте переменные окружения

1. Скопируйте файл с примером:
```bash
copy Документация\Конфигурация\.env.example .env
```

2. Откройте `.env` и добавьте ваш API ключ:
```env
GRS_AI_API_KEY=ваш_ключ_api_здесь
GRS_AI_API_URL=https://grsaiapi.com
```

> 💡 **Где взять API ключ?** Получите его в личном кабинете GRS AI: https://grsai.com/dashboard

### Шаг 3: Проверьте работу

Запустите тестовый скрипт:

```bash
python docs/scripts/scripts/test_grs_ai.py
```

Если все настроено правильно, вы увидите результаты 7 тестов, включая:
- ✓ Простой запрос
- ✓ Системный промпт
- ✓ Диалог с контекстом
- ✓ Потоковый режим
- ✓ Разные модели
- ✓ Список моделей
- ✓ Обработка ошибок

## 💻 Первая программа

Создайте файл `test_ai.py`:

```python
from blocks.ai_integrations import GRSAIClient

# Создание клиента
client = GRSAIClient()

# Простой запрос
response = client.simple_ask("Привет! Расскажи о себе в двух предложениях.")
print(response)
```

Запустите:
```bash
python test_ai.py
```

## 📚 Основные возможности

### 1. Простые запросы

```python
from blocks.ai_integrations import GRSAIClient

client = GRSAIClient()

# Генерация заголовка
title = client.simple_ask(
    question="Придумай заголовок для статьи про Python",
    system_prompt="Ты профессиональный копирайтер"
)
print(title)
```

### 2. Диалоги с контекстом

```python
messages = [
    {"role": "system", "content": "Ты помощник по написанию контента"},
    {"role": "user", "content": "Мне нужна статья про AI"},
    {"role": "assistant", "content": "О каком аспекте AI?"},
    {"role": "user", "content": "Про нейронные сети"}
]

response = client.chat(messages=messages)
print(response)
```

### 3. Потоковая генерация

```python
print("Генерация статьи:")
for chunk in client.chat_stream(
    messages=[{"role": "user", "content": "Напиши короткую историю"}]
):
    print(chunk, end="", flush=True)
```

### 4. Выбор модели

```python
# Быстрая модель для простых задач
response = client.simple_ask("Привет!", model="gpt-4o-mini")

# Мощная модель для сложных задач
response = client.simple_ask(
    "Напиши развернутый анализ...",
    model="gemini-2.5-pro"
)
```

## 🎯 Создание своего блока

Пример блока для генерации заголовков:

```python
# blocks/content_creation/titles.py
from blocks.ai_integrations import GRSAIClient

class TitlesGeneratorBlock:
    def __init__(self):
        self.ai_client = GRSAIClient()
    
    def execute(self, content_item):
        """Генерация заголовков"""
        
        prompt = f"""
        Тема: {content_item.topic}
        Аудитория: {content_item.target_audience}
        
        Сгенерируй 5 цепляющих заголовков.
        """
        
        response = self.ai_client.simple_ask(
            question=prompt,
            system_prompt="Ты профессиональный копирайтер",
            model="gpt-4o-mini"
        )
        
        # Сохранение результата
        content_item.metadata['titles'] = response.split('\n')
        
        return content_item
```

Подробнее: [blocks/ai_integrations/USAGE_IN_BLOCKS.md](blocks/ai_integrations/USAGE_IN_BLOCKS.md)

## 📖 Документация

### Основная документация
- [README.md](README.md) - Обзор проекта
- [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура системы
- [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) - Структура файлов
- [BLOCKS_REGISTRY.md](BLOCKS_REGISTRY.md) - Реестр блоков

### GRS AI Client
- [blocks/ai_integrations/README.md](blocks/ai_integrations/README.md) - Полная документация клиента
- [blocks/ai_integrations/QUICK_REFERENCE.md](blocks/ai_integrations/QUICK_REFERENCE.md) - Краткий справочник
- [blocks/ai_integrations/USAGE_IN_BLOCKS.md](blocks/ai_integrations/USAGE_IN_BLOCKS.md) - Примеры использования
- [GRS_AI_API_INTEGRATION.md](GRS_AI_API_INTEGRATION.md) - Документация API

### Схемы данных
- [CONTENT_ITEM_SCHEMA.md](CONTENT_ITEM_SCHEMA.md) - Схема контент-элемента

## 🔧 Доступные модели

### Быстрые модели (для простых задач)
- `gpt-4o-mini` ⭐ **рекомендуется для большинства задач**
- `gemini-2.5-flash` - очень быстрая
- `gemini-2.5-flash-lite` - самая быстрая

### Мощные модели (для сложных задач)
- `gemini-2.5-pro` ⭐ **для длинных статей и анализа**
- `gemini-3-pro` - новейшая версия
- `gpt-4o-all` - универсальная

### Простые модели
- `nano-banana-fast` - для очень простых задач
- `nano-banana` - базовая

## 🎨 Примеры задач

### Генерация заголовков
```python
titles = client.simple_ask(
    "Придумай 5 заголовков для статьи про искусственный интеллект",
    system_prompt="Ты копирайтер",
    model="gpt-4o-mini"
)
```

### Написание статьи
```python
article = ""
for chunk in client.chat_stream(
    messages=[
        {"role": "system", "content": "Ты автор статей"},
        {"role": "user", "content": "Напиши статью про Python (1500 слов)"}
    ],
    model="gemini-2.5-pro"
):
    article += chunk
```

### Генерация хештегов
```python
hashtags = client.simple_ask(
    f"Сгенерируй 10 хештегов для поста про: {text}",
    system_prompt="Ты SMM-специалист",
    model="gpt-4o-mini"
)
```

### SEO-оптимизация
```python
seo = client.simple_ask(
    f"Создай meta description и keywords для статьи: {text}",
    system_prompt="Ты SEO-специалист",
    model="gpt-4o-mini"
)
```

## ⚠️ Troubleshooting

### Проблема: "API key must be provided"
**Решение:** Убедитесь, что в `.env` файле установлена переменная `GRS_AI_API_KEY`

### Проблема: Абракадабра вместо кириллицы
**Решение:** Клиент автоматически исправляет кодировку. Если проблема сохраняется, проверьте версию `requests`

### Проблема: Timeout
**Решение:** Увеличьте timeout в конфигурации:
```python
from blocks.ai_integrations.grs_ai_client import GRSAIConfig

config = GRSAIConfig(api_key="key", timeout=120)
client = GRSAIClient(config=config)
```

### Проблема: Пустой ответ
**Решение:** Клиент автоматически пробует fallback модели. Убедитесь, что `use_fallback=True`

## 🎓 Дальнейшее изучение

1. **Изучите примеры блоков** в [USAGE_IN_BLOCKS.md](blocks/ai_integrations/USAGE_IN_BLOCKS.md)
2. **Прочитайте документацию API** в [GRS_AI_API_INTEGRATION.md](GRS_AI_API_INTEGRATION.md)
3. **Создайте свой первый блок** для генерации контента
4. **Изучите архитектуру** в [ARCHITECTURE.md](ARCHITECTURE.md)

## 💡 Полезные ссылки

- **Официальная документация GRS AI:** https://grsai.com/dashboard/documents/chat
- **Личный кабинет (для получения API ключа):** https://grsai.com/dashboard
- **Реестр блоков:** [BLOCKS_REGISTRY.md](BLOCKS_REGISTRY.md)

## 🤝 Следующие шаги

1. ✅ Установили зависимости
2. ✅ Настроили API ключ
3. ✅ Протестировали клиент
4. ⏭️ Создайте свой первый блок для генерации контента
5. ⏭️ Интегрируйте блок в пайплайн
6. ⏭️ Добавьте автопостинг на нужные площадки

---

**Готовы начать?** Запустите `python docs/scripts/scripts/test_grs_ai.py` и убедитесь, что все работает! 🚀
