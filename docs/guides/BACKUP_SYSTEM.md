# Система бекапов ContentZavod

Система управления резервными копиями блоков для безопасной разработки и возможности отката к стабильным версиям.

---

## 🎯 Назначение

Система бекапов позволяет:
- ✅ Сохранять рабочие версии блоков перед внесением изменений
- ✅ Откатываться к стабильным версиям при необходимости
- ✅ Отслеживать историю изменений блоков
- ✅ Сравнивать текущую версию с предыдущими
- ✅ Помечать версии тегами (например, "working", "stable", "v1.0")

---

## 📦 Структура

```
ContentZavod/
├── backups/                    # Директория с бекапами
│   ├── backup_0001/           # Первый бекап
│   │   ├── metadata.json      # Метаданные бекапа
│   │   └── grs_ai_client.py   # Файл бекапа
│   ├── backup_0002/           # Второй бекап
│   │   ├── metadata.json
│   │   └── titles.py
│   └── metadata.json          # Общие метаданные всех бекапов
└── scripts/
    └── backup_manager.py      # Утилита управления бекапами
```

---

## 🚀 Использование

### Создание бекапа

```bash
# Базовое создание бекапа
python scripts/backup_manager.py create blocks/ai_integrations/grs_ai_client.py "Рабочая версия с fallback"

# С тегами
python scripts/backup_manager.py create blocks/ai_integrations/grs_ai_client.py "Стабильная версия v1.0" --tags stable,v1.0

# Короткая команда без описания
python scripts/backup_manager.py create blocks/content_creation/titles.py
```

**Вывод:**
```
✅ Бекап создан: backup_0001
   Файл: blocks/ai_integrations/grs_ai_client.py
   Размер: 24.5 KB
   Описание: Рабочая версия с fallback
   Теги: stable, v1.0
```

### Просмотр списка бекапов

```bash
# Все бекапы
python scripts/backup_manager.py list

# Бекапы конкретного файла
python scripts/backup_manager.py list blocks/ai_integrations/grs_ai_client.py
```

**Вывод:**
```
ID              Файл                                     Дата                 Размер     Описание
========================================================================================================================
backup_0003     blocks/content_creation/titles.py        2026-02-01 15:30:00  5.2 KB     Добавлена валидация
backup_0002     blocks/ai_integrations/grs_ai_client.py  2026-02-01 14:20:00  24.5 KB    Рабочая версия с fallback
backup_0001     blocks/ai_integrations/grs_ai_client.py  2026-02-01 12:00:00  23.1 KB    Первая рабочая версия

Всего бекапов: 3
```

### Информация о бекапе

```bash
python scripts/backup_manager.py info backup_0001
```

**Вывод:**
```
📦 Бекап: backup_0001
   Файл: blocks/ai_integrations/grs_ai_client.py
   Имя файла: grs_ai_client.py
   Размер: 24.5 KB
   Хеш: a1b2c3d4e5f6...
   Создан: 2026-02-01 12:00:00
   Описание: Первая рабочая версия
   Теги: stable, v1.0
   Путь к бекапу: backups/backup_0001/grs_ai_client.py
```

### Восстановление бекапа

```bash
python scripts/backup_manager.py restore backup_0001
```

**Вывод:**
```
⚠️  Восстановление бекапа: backup_0001
   Файл: blocks/ai_integrations/grs_ai_client.py
   Описание: Первая рабочая версия

Продолжить? (y/n): y
✅ Бекап восстановлен: blocks/ai_integrations/grs_ai_client.py
```

**Важно:** Перед восстановлением автоматически создается бекап текущей версии с тегом `auto-backup`.

### Сравнение с текущей версией

```bash
python scripts/backup_manager.py diff backup_0001
```

**Вывод:**
```
🔍 Сравнение бекапа backup_0001 с текущей версией:
   Статус: different
   Файлы различаются
   Строк в бекапе: 450
   Строк в текущей версии: 480
   Хеш бекапа: a1b2c3d4e5f6...
   Хеш текущей версии: f6e5d4c3b2a1...
```

### Удаление бекапа

```bash
python scripts/backup_manager.py delete backup_0001
```

**Вывод:**
```
⚠️  Удаление бекапа: backup_0001
   Файл: blocks/ai_integrations/grs_ai_client.py

Продолжить? (y/n): y
✅ Бекап удален: backup_0001
```

---

## 📋 Рекомендуемый workflow

### 1. Перед началом работы над блоком

```bash
# Создаем бекап текущей версии
python scripts/backup_manager.py create blocks/content_creation/titles.py "Перед добавлением новой функции" --tags working
```

### 2. После достижения стабильной версии

```bash
# Создаем бекап с тегом stable
python scripts/backup_manager.py create blocks/content_creation/titles.py "Стабильная версия - генерация работает" --tags stable,v1.0
```

### 3. Перед крупным рефакторингом

```bash
# Создаем бекап с описанием
python scripts/backup_manager.py create blocks/ai_integrations/grs_ai_client.py "Перед рефакторингом парсинга ответов" --tags before-refactor
```

### 4. При возникновении проблем

```bash
# Смотрим список бекапов
python scripts/backup_manager.py list blocks/content_creation/titles.py

# Сравниваем с последней рабочей версией
python scripts/backup_manager.py diff backup_0005

# Восстанавливаем рабочую версию
python scripts/backup_manager.py restore backup_0005
```

---

## 🏷️ Рекомендуемые теги

| Тег | Назначение | Пример |
|-----|------------|--------|
| `working` | Рабочая версия | Блок работает, но может требовать улучшений |
| `stable` | Стабильная версия | Полностью протестирована, готова к использованию |
| `v1.0`, `v1.1` | Версия | Нумерация версий блока |
| `before-refactor` | Перед рефакторингом | Сохранение перед крупными изменениями |
| `auto-backup` | Автоматический бекап | Создается автоматически при восстановлении |
| `milestone` | Важная веха | Достижение важного этапа разработки |
| `production` | Продакшн версия | Версия, используемая в продакшене |

---

## 🔍 Примеры использования

### Пример 1: Разработка нового блока

```bash
# Создали первую версию блока
python scripts/backup_manager.py create blocks/content_creation/titles.py "Первая версия - базовая генерация" --tags v0.1

# Добавили валидацию
python scripts/backup_manager.py create blocks/content_creation/titles.py "Добавлена валидация заголовков" --tags v0.2

# Блок работает стабильно
python scripts/backup_manager.py create blocks/content_creation/titles.py "Стабильная версия" --tags stable,v1.0,working
```

### Пример 2: Рефакторинг существующего блока

```bash
# Сохраняем текущую рабочую версию
python scripts/backup_manager.py create blocks/ai_integrations/grs_ai_client.py "Перед оптимизацией парсинга" --tags before-refactor,working

# ... делаем рефакторинг ...

# Если что-то пошло не так
python scripts/backup_manager.py list blocks/ai_integrations/grs_ai_client.py
python scripts/backup_manager.py restore backup_0010
```

### Пример 3: Тестирование разных подходов

```bash
# Сохраняем базовую версию
python scripts/backup_manager.py create blocks/content_creation/article_text.py "Базовая версия с gpt-4o-mini" --tags approach-1

# Пробуем другой подход
# ... меняем код ...
python scripts/backup_manager.py create blocks/content_creation/article_text.py "Версия с gemini-2.5-pro" --tags approach-2

# Сравниваем результаты и выбираем лучший
python scripts/backup_manager.py list blocks/content_creation/article_text.py
```

---

## ⚙️ Технические детали

### Метаданные бекапа

Каждый бекап содержит следующие метаданные:

```json
{
  "backup_id": "backup_0001",
  "file_path": "blocks/ai_integrations/grs_ai_client.py",
  "file_name": "grs_ai_client.py",
  "file_hash": "a1b2c3d4e5f6...",
  "file_size": 25000,
  "description": "Рабочая версия с fallback",
  "tags": ["stable", "v1.0"],
  "created_at": "2026-02-01T12:00:00",
  "backup_file": "backups/backup_0001/grs_ai_client.py"
}
```

### Хеширование

Система использует MD5 хеш для определения идентичности файлов. Если вы пытаетесь создать бекап файла с таким же содержимым, система предупредит вас.

### Автоматические бекапы

При восстановлении бекапа автоматически создается бекап текущей версии с тегами `auto-backup` и `before-restore`. Это позволяет откатить восстановление, если что-то пошло не так.

---

## 🛡️ Безопасность

### Что сохраняется в Git

- ✅ `backups/.gitkeep` - пустой файл для сохранения структуры
- ❌ `backups/backup_*/` - сами бекапы НЕ сохраняются в Git
- ❌ `backups/metadata.json` - метаданные НЕ сохраняются в Git

Бекапы являются локальными и не коммитятся в репозиторий.

### Рекомендации

1. **Регулярно создавайте бекапы** перед внесением изменений
2. **Используйте осмысленные описания** для быстрого поиска нужной версии
3. **Помечайте стабильные версии** тегом `stable`
4. **Не удаляйте бекапы** без необходимости - они занимают мало места
5. **Проверяйте diff** перед восстановлением бекапа

---

## 🔧 Интеграция с разработкой

### В начале работы над блоком

```bash
# Создаем первый бекап
python scripts/backup_manager.py create blocks/new_block/block.py "Начальная версия" --tags v0.1
```

### После каждого важного этапа

```bash
# Блок работает
python scripts/backup_manager.py create blocks/new_block/block.py "Базовая функциональность работает" --tags working

# Добавлена новая фича
python scripts/backup_manager.py create blocks/new_block/block.py "Добавлена обработка ошибок" --tags v0.2

# Стабильная версия
python scripts/backup_manager.py create blocks/new_block/block.py "Готов к использованию" --tags stable,v1.0
```

### Перед коммитом в Git

```bash
# Создаем финальный бекап перед коммитом
python scripts/backup_manager.py create blocks/new_block/block.py "Версия для коммита" --tags production,v1.0
```

---

## 📊 Статистика и мониторинг

### Просмотр всех бекапов

```bash
python scripts/backup_manager.py list
```

### Фильтрация по файлу

```bash
python scripts/backup_manager.py list blocks/ai_integrations/grs_ai_client.py
```

### Поиск по тегам (будущая функция)

```bash
# Планируется в будущих версиях
python scripts/backup_manager.py list --tags stable
python scripts/backup_manager.py list --tags v1.0
```

---

## 🐛 Troubleshooting

### Проблема: "Файл не найден"

**Решение:** Убедитесь, что путь указан относительно корня проекта:
```bash
# Правильно
python scripts/backup_manager.py create blocks/ai_integrations/grs_ai_client.py

# Неправильно
python scripts/backup_manager.py create ../blocks/ai_integrations/grs_ai_client.py
```

### Проблема: "Бекап с таким же содержимым уже существует"

**Решение:** Это нормально - система защищает от дублирования. Если вы действительно хотите создать новый бекап, внесите хотя бы минимальное изменение в файл.

### Проблема: Не могу восстановить бекап

**Решение:** Проверьте, что бекап существует:
```bash
python scripts/backup_manager.py info backup_0001
```

---

## 📝 Changelog системы бекапов

### [2026-02-01] - Первая версия

**Добавлено:**
- ✅ Создание бекапов с описанием и тегами
- ✅ Просмотр списка бекапов
- ✅ Восстановление бекапов с автоматическим созданием бекапа текущей версии
- ✅ Сравнение бекапа с текущей версией
- ✅ Удаление бекапов
- ✅ Хеширование для определения идентичности файлов
- ✅ Метаданные для каждого бекапа

**Планируется:**
- ⏳ Фильтрация по тегам
- ⏳ Экспорт/импорт бекапов
- ⏳ Автоматическое создание бекапов по расписанию
- ⏳ Web-интерфейс для управления бекапами

---

## 🔗 См. также

- [README.md](README.md) - Главная документация проекта
- [GETTING_STARTED.md](GETTING_STARTED.md) - Быстрый старт
- [PROJECT_STATUS.md](PROJECT_STATUS.md) - Статус проекта
- [PATCHES.md](PATCHES.md) — Патчи и история изменений проекта
