# Как запустить Spambot

Краткая инструкция по запуску NewsBot (Spambot) для автопостинга в Telegram.

---

## 🚀 Быстрый запуск (3 шага)

### 1. Настройте .env файл

```bash
# Скопируйте пример
copy Документация\Конфигурация\.env.example .env
```

Откройте `.env` и заполните:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHANNEL_ID=@ваш_канал
```

### 2. Установите зависимости (если нужно)

```bash
pip install -r Документация\Конфигурация\requirements.txt
```

### 3. Запустите бота

**Самый простой способ (двойной клик по файлу в блоке):**
```
Блоки\спамбот\start.bat
```

Готово! Бот начнет работать.

---

## 📋 Все способы запуска

### Способ 1: Из папки блока (рекомендуется)

**Двойной клик на файл в папке блока:**
```
Блоки\спамбот\start.bat
```

**Или из командной строки (из корня проекта):**
```bash
Блоки\спамбот\start.bat
```

### Способ 2: Через полный скрипт

```bash
Документация\Скрипты\scripts\run_spambot.bat
```

### Способ 3: Напрямую через Python (из корня проекта)

```bash
python -m blocks.spambot
```

---

## 🔑 Где взять токены

### Telegram Bot Token

1. Откройте https://t.me/BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен (формат: `1234567890:ABCdef...`)

### Channel ID

1. Создайте публичный канал в Telegram
2. Установите имя канала (например, `flowcabinetnews`)
3. Используйте `@имя_канала` (например, `@flowcabinetnews`)
4. Добавьте бота в администраторы канала с правом публикации

---

## ✅ Что делает BAT-файл

При запуске автоматически:

1. ✅ Проверяет наличие `.env` файла
2. ✅ Загружает переменные окружения
3. ✅ Проверяет токены
4. ✅ Активирует venv (если есть)
5. ✅ Проверяет зависимости
6. ✅ Предлагает установить зависимости (если нужно)
7. ✅ Запускает бота
8. ✅ Обрабатывает ошибки

---

## 🛑 Как остановить бота

Нажмите `Ctrl+C` в окне командной строки.

Бот корректно завершит работу.

---

## ⚠️ Возможные проблемы

### Проблема: "Файл .env не найден"

**Решение:**
```bash
copy Документация\Конфигурация\.env.example .env
# Откройте .env и заполните токены
```

### Проблема: "TELEGRAM_BOT_TOKEN не установлена"

**Решение:**
Откройте `.env` и добавьте:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
```

### Проблема: "Зависимости не установлены"

**Решение:**
```bash
pip install -r Документация\Конфигурация\requirements.txt
```

Или согласитесь на установку при запуске BAT-файла.

### Проблема: "Бот завершился с ошибкой"

**Проверьте:**
1. Токен бота правильный
2. Бот добавлен в администраторы канала
3. У бота есть права на публикацию
4. Интернет соединение работает

---

## 📊 Что делает бот

После запуска бот:

1. 📡 Проверяет RSS-ленты (12 основных + 10 запасных)
2. 🔍 Фильтрует статьи по ключевым словам
3. 🖼️ Выбирает только статьи с изображениями
4. 🌐 Переводит на русский язык
5. 📝 Добавляет случайные хештеги
6. 📤 Публикует в Telegram канал
7. ⏱️ Ждет 2 минуты перед следующим постом

---

## 📖 Дополнительная информация

- **Полная документация:** Блоки/spambot/README.md
- **Быстрый старт:** Блоки/спамбот/QUICK_START.md
- **Ключи и токены:** [KEYS_AND_TOKENS.md](KEYS_AND_TOKENS.md)
- **Правила проекта:** [PROJECT_RULES.md](PROJECT_RULES.md)

---

## 💡 Полезные команды

```bash
# Запуск бота
Блоки\спамбот\start.bat

# Просмотр логов (если перенаправлены)
type storage\logs\newsbot.log

# Остановка бота
# Нажмите Ctrl+C в окне бота

# Проверка зависимостей
pip list | findstr telegram

# Обновление зависимостей
pip install --upgrade -r Документация\Конфигурация\requirements.txt
```

---

**Готово!** Теперь вы можете запустить Spambot одним кликом! 🚀
