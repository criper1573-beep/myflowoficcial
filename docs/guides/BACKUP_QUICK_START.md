# Быстрый старт с системой бекапов

## 🚀 Основные команды

### 1. Создать бекап блока

```bash
python docs/scripts/scripts/backup_manager.py create blocks/ai_integrations/grs_ai_client.py "Описание версии" --tags stable,v1.0
```

### 2. Посмотреть список бекапов

```bash
# Все бекапы
python docs/scripts/scripts/backup_manager.py list

# Бекапы конкретного файла
python docs/scripts/scripts/backup_manager.py list blocks/ai_integrations/grs_ai_client.py
```

### 3. Восстановить бекап

```bash
python docs/scripts/scripts/backup_manager.py restore backup_0001
```

### 4. Сравнить с текущей версией

```bash
python docs/scripts/scripts/backup_manager.py diff backup_0001
```

---

## 📋 Когда создавать бекапы

### ✅ ОБЯЗАТЕЛЬНО создавайте бекап:

1. **После того, как блок заработал** - первая рабочая версия
   ```bash
   python docs/scripts/scripts/backup_manager.py create blocks/content_creation/titles.py "Блок работает - базовая генерация" --tags working,v0.1
   ```

2. **Перед крупными изменениями** - рефакторинг, оптимизация
   ```bash
   python docs/scripts/scripts/backup_manager.py create blocks/ai_integrations/grs_ai_client.py "Перед рефакторингом" --tags before-refactor
   ```

3. **Когда достигнута стабильная версия** - все работает отлично
   ```bash
   python docs/scripts/scripts/backup_manager.py create blocks/content_creation/titles.py "Стабильная версия" --tags stable,v1.0
   ```

4. **Перед переписыванием блока** - сохраняем старую версию
   ```bash
   python docs/scripts/scripts/backup_manager.py create blocks/old_block/block.py "Старая версия перед переписыванием" --tags archive
   ```

### ❌ НЕ создавайте бекап:

- После каждой мелкой правки (используйте Git для этого)
- Если блок еще не работает и вы только начали его писать
- Если изменения минимальны и не влияют на функциональность

---

## 🏷️ Рекомендуемые теги

| Тег | Когда использовать |
|-----|-------------------|
| `working` | Блок работает, можно использовать |
| `stable` | Полностью протестирован, стабилен |
| `v1.0`, `v1.1` | Версионирование |
| `before-refactor` | Перед крупными изменениями |
| `milestone` | Важный этап разработки |

---

## 💡 Примеры workflow

### Пример 1: Разработка нового блока

```bash
# Шаг 1: Написали блок, он работает
python docs/scripts/scripts/backup_manager.py create blocks/content_creation/titles.py "Первая рабочая версия" --tags working,v0.1

# Шаг 2: Добавили новую функцию, все работает
python docs/scripts/scripts/backup_manager.py create blocks/content_creation/titles.py "Добавлена валидация" --tags working,v0.2

# Шаг 3: Блок стабилен, готов к использованию
python docs/scripts/scripts/backup_manager.py create blocks/content_creation/titles.py "Стабильная версия" --tags stable,v1.0
```

### Пример 2: Рефакторинг существующего блока

```bash
# Шаг 1: Сохраняем текущую рабочую версию
python docs/scripts/scripts/backup_manager.py create blocks/ai_integrations/grs_ai_client.py "Перед оптимизацией" --tags before-refactor,working

# Шаг 2: Делаем рефакторинг...

# Шаг 3: Если что-то сломалось - восстанавливаем
python docs/scripts/scripts/backup_manager.py list blocks/ai_integrations/grs_ai_client.py
python docs/scripts/scripts/backup_manager.py restore backup_0005
```

### Пример 3: Переписывание блока

```bash
# Шаг 1: Сохраняем старую версию
python docs/scripts/scripts/backup_manager.py create blocks/old_block/block.py "Старая версия перед переписыванием" --tags archive,old-version

# Шаг 2: Переписываем блок полностью...

# Шаг 3: Новая версия работает
python docs/scripts/scripts/backup_manager.py create blocks/old_block/block.py "Новая версия после переписывания" --tags working,v2.0
```

---

## 🔍 Полезные команды

### Просмотр информации о бекапе

```bash
python docs/scripts/scripts/backup_manager.py info backup_0001
```

### Удаление бекапа

```bash
python docs/scripts/scripts/backup_manager.py delete backup_0001
```

### Сравнение версий

```bash
# Сравнить бекап с текущей версией
python docs/scripts/scripts/backup_manager.py diff backup_0001
```

---

## 📖 Полная документация

Подробная документация: [BACKUP_SYSTEM.md](BACKUP_SYSTEM.md)

---

## ⚠️ Важно помнить

1. **Бекапы локальные** - они не сохраняются в Git
2. **Создавайте бекапы осознанно** - только когда блок на важном этапе
3. **Используйте осмысленные описания** - чтобы потом понять, что это за версия
4. **Помечайте стабильные версии** тегом `stable`
5. **Перед восстановлением** автоматически создается бекап текущей версии

---

## 🎯 Первый бекап GRS AI Client

После создания системы бекапов, первое что нужно сделать - создать бекап текущей рабочей версии GRS AI Client:

```bash
python docs/scripts/scripts/backup_manager.py create blocks/ai_integrations/grs_ai_client.py "Первая стабильная версия с fallback и обработкой кодировки" --tags stable,v1.0,working
```

Это создаст точку отката на случай, если что-то пойдет не так при дальнейшей разработке.
