# Скопируй этот файл на сервер как .env в корень проекта и заполни значения.
# На сервере: cp env.server.example .env  затем nano .env

# Минимум для дашборда аналитики (остальное — по мере необходимости)
ANALYTICS_DB_PATH=storage/analytics.db

# Для Telegram Mini App: сюда потом вписать HTTPS-адрес дашборда (после настройки Nginx/SSL)
# DASHBOARD_PUBLIC_URL=https://analytics.твой-домен.ru

# Watchdog: при падении сервиса бот пришлёт сюда алерт (узнать свой chat_id: @userinfobot)
# TELEGRAM_ALERT_CHAT_ID=123456789

# Остальные ключи (GRS_AI, Telegram, Google и т.д.) — скопируй из своего локального .env
# если на сервере будут крутиться боты или автопостинг.
